<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE"/>
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK"/>

    <permission
        android:name="com.motorro.background.PERMISSION"
        android:permissionGroup="android.permission-group.READ_MEDIA_AURAL"
        android:protectionLevel="signature" />

    <application
        android:allowBackup="true"
        android:name=".App"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Android">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Android">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".pages.service.TimerService"
            android:foregroundServiceType="mediaPlayback"
            android:enabled="true"
            android:permission="com.motorro.background.PERMISSION"
            android:exported="true">
            <intent-filter>
                <action android:name="com.motorro.background.BIND" />
            </intent-filter>
        </service>

    </application>

</manifest>